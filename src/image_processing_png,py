import numpy as np
from PIL import Image

def image_matrix(filename):
    im = np.asarray((Image.open(filename)).convert("RGBA")).astype(float)
    
    red_im = im[:,:,0]
    green_im = im[:,:,1]
    blue_im = im[:,:,2]
    s_im = im[ : :,3]
    
    return red_im, green_im, blue_im,s_im